#include <iostream>
#include "BMI.h"

int main() {
    std::cout<<male_calc((float)197.9,(float)40.2)<<"\n";
    /*
     * Proc take input file and each line is a new person with a def of:
     * {Index, Gender, Details}
     *
     * When the program start it asks would you like to upload a formatted file or enter information (size)
     * 1. Ask file or input w/ size
     *
     *
     */
    return 0;
}